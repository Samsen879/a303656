# Phase-D self-audit — not an independent referee report

| Proof obligation / common overclaim | This package |
|---|---|
| Treat source's informal "gateway" as an unambiguous direct-edge condition | Formalizes basal Γ={x:O(x)={3}} and separates the stronger edge-only class. Source necessary implication is used only at its supported scope. |
| Drop an odd order factor not on one convenient path | D(q) is full absolute admitted closure; terminal set includes every branch. |
| First extension only has order 7 | Uses all four orders 7,14,21,42 in the necessary basal class. |
| Count factor 7 of Φ42 as exact order 42 | Rejects it using exact order 6; adversarial regression tests this. |
| Probable-prime output is a proof | All 11 certified products use 108 recursive full-(n−1) Lucas prime certificates. |
| One factor function discovers and verifies | Verification has no SymPy import; integer recursion/Möbius products, Lucas checks, two modular cores, and small independent trial division. |
| Require prime-minus-one squarefree | Only exact orders are squarefree. Regular 519499 has 7² dividing p−1. |
| Polynomial derivative proves integer-value squarefreeness | Explicitly rejected; primitive roots are simple even when the fixed integer lift is Wieferich. |
| 48 orders cover all height-two branching | They cover exactly |D|=3. General height-two gate has 508 potential indices before the first four exclusions. |
| Call an untested template an actual nonregular root | No actual B7 member found. The 89-digit prime and six gate relays are all regular. |
| Claim actual B7 proper-subset reduction while every B7 is retained | Strictness is stated for the candidate representation domain; actual-set filtering equality is explicitly noted. |
| Shared terminal 3 is a forbidden shared relay | D excludes terminal 3; only admitted nodes >3 are subject to basin disjointness. |
| Fix depth-three heads to 271/4159/31051 in a necessary theorem | Retains all six depth-three heads and 20 possible triples. H0 is only the conditional construction. |
| Nonregular q assumed in filling theorem silently supplied | D5 has an explicit existential arithmetic hypothesis; actual regular skeleton retains a hole. |
| Fresh residues for different fibers or anchors | D5 fixes a single a and one residue per row, then uses CRT over distinct prime squares. |
| Coarse U misses a regular relay's private digit | Every proper relay has a parent whose order contains its prime; thus U=L at K=2 in the conditional filled basin. |
| Local zero counted as fatal | Actual skeleton checks v modp² nonzero and divisible by p. Hole remains. |
| 301/602 table factors promoted before local primality proofs | Kept only in discovery_only_301_602.json; verifier excludes neither index. |
| Finite branching implies finite total tree | Explicitly rejected; no uniform height or vertex bound established. |
| Infer eight nonregular roots from additional helpers | Extra gate vertex is regular. The separate conditional N=7 bound is at least16 odd rows, at least9 regular; not N≥8. |
| N>7 may contain a seven-origin local witness | Irrelevant to total ≥8 upgrade, which would require ruling out total N=7. This has not been done. |
| Full repository/read requirement claimed complete | Individual core-file scope disclosed. No entire-directory file-by-file reading or full checkout/replay claimed. |
| Producer's tests called independent-author review | Clearly labeled same-session computational independence only. |

Main universal arithmetic statements are proposed proofs D1–D5 in REPORT.md. Regression tests support finite identities and reject malformed certificates, but do not prove the unbounded statements by sampling.

Final verdict: no B7 emptiness proof, no actual B7 member, no lower-bound upgrade, no GitHub writes.
