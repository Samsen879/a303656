# Repository-native integration receipt

This directory integrates the ordinary files from
`A303656_SCAN_AND_SEVEN_PRIME_AUDIT.zip`. The transport ZIP and its nested
Unified source ZIP are not committed. `evidence/nested_source_receipt.json`
records the byte identity of the omitted nested ZIP with the separately
supplied Unified package.

The seven-prime result is an unbounded structural theorem, independently
derived here as an audit route. The `q <= 2,000,000,000` inventory is a bounded
exact computation. Its corollaries do not convert the scan into a universal
result. Derived provenance macros are not original actual primes or arithmetic
resources.

```text
PROJECT: PAUSED
ACTIVE PROMOTED ROUTE: NONE
A303656: UNRESOLVED
REPOSITORY INTEGRATION: DRAFT PR ONLY
```
