# Historical rediscovery and source limitations

J producer did not have I4. J-W1's residual ratio/positive clipping is retained
only as a reformulation of PW-03/PW-04's witness-sensitive structure. Its
additional norm-ratio margin is not declared independent theorem novelty.
J-K1's pair completion and K2's inherited pair identity point to I4 AUX-01.
Original source labels inside historical code/results are not second authority
records. J reference is retained only as a historical finite replay path.

I2-L2 already contains the global CRT no-progression-of-norms observation.
J-M1/M2 add an explicit finite interval/feature-period qualification; only
that quantitative strengthening is curated. This prevents claiming the global
background observation as a fresh theorem.

The archive `A303656_EXTREMAL_INVERSE_PHASE_K.zip` has reports/code headers
calling themselves Phase K2. Its CONTENT is the local7/11 classification layer;
the distinct LARGE_PRIME_COMPLETION_PHASE_K2 archive is the later completion
audit. Actual filenames/hashes identify sources; original headers are unedited.

Source K did not retrieve the raw pilot: it checked six named grids and reported
window rankings as brief input. L0 did retrieve and replay the actual saved
pilot arrays/rankings/retained identities in a disposable copy. Those are
separate evidence paths; K's historical null/unavailable fields stay null.
No fine-subclass empirical independence test is retroactively invented.

Pilot least-bad7/11 recurrence is FILTERED by L2/L3; unfiltered histograms often
have prime3 first. This is a definition correction, not inconsistent arithmetic.
Pilot strict-positive R differs from I2/K/K2 inclusive R; production has no
equality pairs so its window extrema are comparable, but baseline n=2 is not
silently called an original counterexample.

LC-K's beta=1/2 remains open; K2-N1 refutes beta<1/2 only. ML-K2 is weaker and
unproved. No source assessment is promoted into a universal mathematical result.
