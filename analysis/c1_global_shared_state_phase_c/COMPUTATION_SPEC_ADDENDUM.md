# Addendum before the additional lift checks

After obtaining the coarse configuration results, verify one actual full-period
common escape for each of the 5859 frozen global state choices, without enumerating
L. Select the smallest coarse escape a and solve d=a (mod 1302),
d=0 (mod 43*1303). Directly test all five original rows and both anchors modulo
q^2. Fixed residues r43=r1303=2 make coordinate-zero a safe private lift for
each of the two support rows. This is an independent direct arithmetic check of
these witnesses, not a full-L exhaustive cover computation.

Add named unit tests for the six-type distinctions, genuine support U, actual
non-TU minor, local-zero exclusion, multi-constraint valuation logic, state
collision, and agreement of the two resultant organizations. No domain of the
unbounded theorems is enlarged on the basis of the tests.

## Order-collapse supplement, before execution

For the fixed resultant triples Y={0,1,2}, their difference gcd is 1.
At h=p in {3,5,7}, verify V_(p,1)(1)=Phi_p(5) by exact division,
complete trial factorization, prime certification, exact orders and lifting.
For q=20771 at original h=67, additionally compare Y=7 and Y=162:
the ordinary representatives differ by u=155, so the same paired state r=16
must remain legal in both. Verify q^2 divides V_(67,1)(155) by modular
numerator/denominator inversion, without creating its enormous integer value.
This supplements, not replaces, the incompatible Y=7 versus Y=8 check.

## Accepted-set sensitivity, before execution

For the actual regular row q=3, K=4 and 5, enumerate all residues and the
three nonempty allowed sets E={1}, {3}, {1,3}. Compare direct fatal valuations
with the dynamic-center formula on the full row period. Verify that omission
of valuation 1 permits all first digits in each active lower guard. Verify
that all three lifts of the centered first digit modulo 9 occur in the common
safe projection for every permitted E. Separately include a deliberately
INVALID E={1,2}, K=3, r=2 regression to demonstrate why accepting even valuation
2 would invalidate the width lemma. It is not an admitted arithmetic example.

## Named hereditary examples, before execution

Certify the exact signatures and recursive H-membership of the explicitly named
regular examples 3,7,11,19,31,43,67,127,1303. Reject 5 as inadmissible before
attempting a multiplicative order with base 5. This is a fixed finite named
panel supporting the theorem's illustrative signatures, not a prime scan.
