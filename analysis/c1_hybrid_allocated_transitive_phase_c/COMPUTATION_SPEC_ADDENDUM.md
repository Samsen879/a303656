# Addendum frozen before extended checks

The paired low-residue search found q=1645333507, r_low=6,
b0=1, b1=85648253. No prime scan is authorized.

For an actual coalescence regression add the regular rows 11,23,67, K=2,
E={1}, r=2, to the panel already specified. The panel is now
{3,11,23,67,20771,40487,30469139,1645333507}.
Use 11->5 and 23,67->11, with all available selected-anchor divisibility
guards targeted. Keep original row identity, deduplicate geometric cylinders.
Use q20771,q40487 residues q+2, and relay30469139 residue6.
Select the first h in {0,1,2} making both q1645333507 anchors rigid fatal.
Compute final U/L from ALL rows, never from the chosen path alone.

Exact-union audit: all 4096 subsets of the 12 depth1/depth2 cylinders over
Z/27 and all 27 centers, with J in empty,{0},{2},{0,2},{1}.
Compare ancestor-pruned prefix cylinders against direct residue masks.

Abstract hybrid theorem regression: coordinates3,5,7; relay at5 with a
single active anchor guard at3 and a proper centered dynamic shell at5.
A mandatory rigid root has guard on3,5,7. Enumerate guard residues,
centers, and direct-to3 versus relay-to5 target allocations, with optional
relay release at3. Include a second root sharing the relay in a separate
exact coalescing family. Models are triangular abstract systems, NOT actual
base-5 nonregular-prime systems. Check all accepted certificates against
independent full-grid direct predicates.

Additional finite checks: nonmatroid exchange for proper-union bundles;
state-conflict with one shared state; 2-adic common-safe failure; no phantom
relay or macro-as-row acceptance. Scope may be reduced but no extra claimed
counts without execution.
