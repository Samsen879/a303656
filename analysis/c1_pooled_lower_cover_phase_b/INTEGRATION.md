# Repository-native integration receipt

This directory integrates the ordinary files from
`A303656_POOLED_LOWER_COVER_PHASE_B.zip`. Research and integration authority
are recorded separately in `manifest.json`.

The allocated-blocker theorem permits an original dynamic row on the blocker
coordinate when the exact full-depth inequality `D_p + mu(B_p) < 1` holds. For
the boundary resource `1645333507 -> coordinate 3`, the recorded load is
`3/4 + 2/27 = 89/108 < 1`. The exact cyclotomic certificate gives
`R_(3,4)=empty`; together with earlier depths this supports only the stated
rank-3 resource taxes.

The six demands FALSE, A0, A1, EITHER, BOTH, TRUE remain distinct. In
particular, EITHER is not BOTH or TRUE. Derived macros are not actual rigid
prime resources. The retained P5 seed construction is conditional and is not
a complete simultaneous certificate or a blanket pooled no-go.

```text
PROJECT: PAUSED
ACTIVE PROMOTED ROUTE: NONE
A303656: UNRESOLVED
REPOSITORY INTEGRATION: DRAFT PR ONLY
```
