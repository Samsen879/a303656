# Architecture and original-row identity

The source I6 conditional N=8 construction splits the old single 31-duty between
two distinct origins on opposite parities. Thus the exactly-seven O31/B31
single-basin condition is not forced by this sufficient N>7 architecture.
This is NOT an actual N=8 certificate or a theorem excluding an additional,
unknown fixed-base necessity argument. Eight terminals remain uninstantiated.

Source I7 freezes the N=9 ternary frontier `(1,5,3)`:

Current arithmetic status: this frozen I7 architecture is CLOSED BY
Sq(279)=empty; see [exact closure proof](c279_closure/PROOF.md). The formal
schema is retained unchanged as historical structural evidence. General N=9
and general N>7 remain UNRESOLVED; no active architecture is promoted.

- depth1: `1 mod3`;
- depth2: `0,3,6,2,5 mod9`;
- depth3: `8,17,26 mod27`.

Three DISTINCT original nonregular roots of exact order `279=9*31`, with logs
`0,93,186 mod279`, supply `0,3,6 mod9` on both parities. The shared actual regular
helper31 has one state `r31=4 mod31²` and is coalesced once, not billed as three
independent providers. The final depth-two providers are the distinct roots.
The other root orders are `903,191,5167,271,4159,31051`.

All root slots use K=2, E={1}; root residues at anchor1 are
`3+5^a+q modq²`. Actual regular support and the frozen canonical logs/residues
are in `reference/producer/conditional_architectures.json`. The source I3/I4
proofs justify canonical filling and gluing only when shared original rows
have identical K,E,r. Same-state BOTH must be checked, not pooled EITHER.

The numerical ledger in `candidate_ledger.json` has exactly THREE actual roots
20771,40487,1645333507: N=3, not N=8 or N=9. It has 27648 quotient cells,
24042 passing BOTH and 3606 failures, and an explicit escape. Quotient cells
have unequal weights: the passing-cell fraction is not a period density.
N8/N9 formal-guard checks are separately conditional and certify zero actual
terminal primes. N9 has nine roots + nine regular helpers above3 + row3,
nineteen conditional odd rows; N counts nonregular original primes, not roles.

Source I9: even a future complete C=1 ledger excludes only the tested anchors
c=0,1 (and canonical two-adic state also all even c). Odd c>=3 needs a further
bridge or an all-exponents explicit-n audit before any original counterexample.
No global classification of every N8/N9 DAG, precision, or nonlinear alternative
is claimed. Full proofs and original limitations remain inspectable in the
unaltered `THEOREMS.md`, `STRUCTURAL_NORMAL_FORMS.md`, and `PROOF_AUDIT.md`.
