# A303656 C=1 Saturation–Contraction Tree Classification — Targeted Phase A

## Verdict

```text
TARGETED PARTIAL ADVANCE
PROJECT: PAUSED
ACTIVE PROMOTED ROUTE: NONE
A303656: UNRESOLVED
```

This package is an independent mathematical reconstruction and exact reference-computation audit bound to:

```text
repository: Samsen879/a303656
repository ID: 1333945235
main SHA: 29fee0317b268d2b2747f9564efc445fdd6da7f9
main tree: 0f5c5acbd5b5d0bc3b24b06cdce70685d10dbd55
```

No GitHub object was modified.

## Main conclusion

The candidate statement has two materially different readings.

1. **Exact semantic reading.**  If a residual state may be an arbitrary finite lower-coordinate predicate, every finite complete triangular coarse cover has a canonical, sound, terminating fiber-elimination tree.  This is proved here, with depth and size bounds.
2. **Strong row-level reading.**  Completeness does **not** by itself imply a beta-one aligned contraction, a multiple-shell aligned partition, or closure of the residual state in the admitted local-mask grammar.  The bridge

   ```text
   density saturation => actual fiber coverage => aligned contraction
   ```

   fails.  An exact admitted arithmetic `67/20771` example has coordinate budget `1` but actual union density `66/67` when the rigid digit is misaligned.

The strongest useful corrected theorem is therefore conditional on **hereditary exact shell decomposability**, including branch-local reuse and admitted residual closure.

## Independent computations

The reference implementation uses the Python standard library only and imports no repository verifier code.  It performs:

- direct full-period enumeration of the `11 -> 67 -> 20771` chain;
- exact aligned/misaligned `67`-fiber comparison;
- local-zero fail-closed and corruption replay;
- the three two-adic boundary regimes and an exhaustive two-anchor residue audit for every `K_2=2,...,10`;
- exhaustive abstract searches for the smallest counterexamples in the stated finite grammar;
- exact semantic contraction evaluation;
- exhaustive universal-projection order checking over all `4096` subsets of a `2 x 3 x 2` product.

## Reproduction

From the package root:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 \
  python3 -m unittest discover -s tests -p 'test_*.py' -v

PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 \
  python3 tools/run_reference.py --output-dir results

PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 \
  python3 tools/run_tests.py --output results/test_results.json
```

To regenerate the manifest, internal hashes, deterministic ZIP, and external checksum sidecar:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 \
  python3 tools/finalize.py \
    --root . \
    --zip ../A303656_C1_CONTRACTION_TREE_PHASE_A.zip
```

The deterministic ZIP uses sorted entries, normalized permissions, a fixed DOS timestamp of `1980-01-01 00:00:00`, and no generated bytecode.

## Package map

- `REPORT.md`: requested final report.
- `DEFINITIONS.md`: contraction state and invariants.
- `THEOREM_AUDIT.md`: proofs, termination, conditional theorem, and logical limits.
- `COUNTEREXAMPLES.md`: arithmetic and abstract counterexamples.
- `PROOF_DEPENDENCIES.md`: independently checked prior dependencies.
- `tools/`: independent reference implementation and deterministic builder.
- `tests/`: arithmetic, boundary, soundness, corruption, and contraction tests.
- `results/`: generated machine-readable evidence.
- `manifest.json`: generated file manifest.
- `SHA256SUMS.txt`: generated internal SHA-256 list.

The outer ZIP SHA-256 is intentionally external to the ZIP and is written to the adjacent `.sha256` sidecar; embedding a ZIP's own digest inside itself is not a well-defined reproducibility requirement.
