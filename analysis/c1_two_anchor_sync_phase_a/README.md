# A303656 two-anchor synchronization Phase A

本目录是通过冻结 source bundle 重建的 repository-native integration。

**结论：C. WEAKER USEFUL BRIDGE FOUND；附带 abstract structural negative results。** Actual H1/H2 仍为 OPEN AFTER TARGETED SEARCH。

主要文件：`REPORT.md`（完整中文数学报告）、`tools/reference.py`（standard-library exact laboratory）、`tests/test_reference.py`（10项回归测试）、`results/source_binding.json`（固定main/PR ancestry/source读取记录）、`results/*.json`（精确反例与结果）。

复现步骤见 `REPRODUCE.md`。冻结代码的 SHA-256 见 `SHA256SUMS.txt`。

三个必要限制：abstract counterexamples 不是 actual admitted systems；actual split example 不是 complete certificate；derived provenance macro 不是 actual prime。

```text
PROJECT: PAUSED
ACTIVE PROMOTED ROUTE: NONE
A303656: UNRESOLVED
REPOSITORY INTEGRATION: DRAFT PR ONLY
```
