# Immutable source custody — 2026-09-14

Artifact: A303656_GAUSSIAN_HURWITZ_DESCENT_PHASE_M.zip
Archive SHA256: 2388a72d15e5bfd16724fe1bc74039d08127b54f3d39bac9304186a0cd26c632
Safe-list: PASS,26 members; internal SHA256SUMS.txt: PASS,25 payload files.
Extraction and all producer working copies were outside the repository.
Original source bytes are unchanged. Original ZIP is not committed.

Live base main: ee56c0d38a626b710354cc81406cb48e1255bdc0
Tree: 7e4924e90e1648101c8aed7b27843c6ba328f160
Default branch main; initially no open PRs. Root PAUSED/NONE/UNRESOLVED unchanged.

Producer checks on a disposable copy:

- reference.py: PASS, all43 frozen shells; exact JSON equality to original results.
- bottlenecks.py: PASS, original barriers/minimax JSON exact equality.
- independent_audit.py: PASS, all43 counts/targets/symmetry orbits/quotient edges,
  H_bad heights and1175 sublevel/path. Distinct stdlib implementation, no producer
  imports/operator tables; same assistant provenance, not an external referee.

Integration producer replay: Python3.12.3, already-installed NumPy2.5.1 in local
venv (original producer recorded Python3.13.5/NumPy2.3.5). Required
integration command uses Python3.10+ STANDARD LIBRARY ONLY. No network, package
installation, new graphs or original-n scan. Optional --with-producer requires
already available NumPy and replays only the original43 shells.

SOURCE_RECEIPTS.json records archive/manifest hashes, bounded replay outcomes
and log hashes. SELECTED_PAYLOAD.json binds each selected committed file's hash,
size and treatment. Ten proof/background documents and three scripts are
BYTE_IDENTICAL_SOURCE_COPY; results.json is DERIVED_COMPACT_CERTIFICATE with
explicit list-filter transformation/source hash. Other Markdown and verify.py
are INTEGRATION SUMMARIES / REPLAY CODE, not purported original source bytes.

Omitted: original ZIP, redundant NPZ cache, full43 detailed graph dump,
exploratory barriers/minimax dumps, duplicate reports and operational logs.
Selected payload is below200KB; no individual new file is above10MB.

Integration replay: python3 -B analysis/gaussian_hurwitz_descent_phase_m/verify.py:
PASS. Optional original43-shell producer replay: PASS.
PASS means finite arithmetic/graph coverage stated by its receipt, not a proof
assistant certification of infinite theorems. Written proofs are retained
inspectably; no mathematical source statement was silently repaired.

LITERATURE.md is source's historical citation/applicability ledger. External
Euclidean/strong-approximation/Hecke/tree statements are NOT promoted as target
hitting authority or required replay inputs; no broad literature theorem is
imported to close a proof. Elementary retained claims have self-contained proofs.
