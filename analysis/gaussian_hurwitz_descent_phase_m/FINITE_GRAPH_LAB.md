# Compact frozen finite certificates

The original producer exhaustively replayed43 selected representation shells:
338,784 raw Hurwitz and116,208 raw Lipschitz points. This is FINITE EVIDENCE,
not an original-n search, universal graph classification or hitting theorem.
Integration replays all43 for custody but commits only these original panels:

| M | purpose |
|---:|---|
|4|small fixed-shell factor-transfer/terminal boundary|
|9|base target-containing and target-free components|
|16|conditional c-lift availability failure|
|47|kappa5=1 is not target sufficiency|
|64|bounded-lift frozen witness, r=3|
|81|separate primitive accessible Omega failure|
|1175|complete finite minimax barrier certificate|

results.json is a DERIVED_COMPACT_CERTIFICATE. Only panel and height-witness
lists are filtered; retained numerical fields/indices/edges are unchanged.
The source hash and exact transformation are in SELECTED_PAYLOAD.json.
The producer operator NPZ is omitted:144 elements and10,368 operators are
regenerated with arbitrary-size Python integers by verify.py.

Every retained graph includes all Hurwitz states of that norm up to signed
coordinate permutations, all legal norm5 moves, unit edges and the specified
finite-symmetry quotient. Primitivity is not imposed on full graphs. Targets
require an actual integral member; any coordinate pair may be moved into w
using explicitly allowed symmetry. Claims are about the augmented graph,
not raw connectivity after removing symmetries.

reference/independent_audit.py independently enumerates pair-of-square states
and reconstructs all quotient edges using36 complete associate representatives.
It reads frozen results as EXPECTATIONS, never imports the first producer,
NumPy or operator tables. Finite independent implementation is not an
external mathematical audit. verify.py also checks target/height equivalence,
Omega heights, both exact products and a minimax calculation/sublevel cut.

Proof text, not sample checks, establishes the infinite content and bounded
lift statements. No new shell, exponent window or original-n scan is run.
