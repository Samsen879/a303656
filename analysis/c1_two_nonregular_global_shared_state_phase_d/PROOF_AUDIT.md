# Internal adversarial audit

This is same-session audit, not independent refereeing.

| Attack | Resolution |
|---|---|
| Both C9 predicates pass but a shared regular residue collides | False in the unrestricted union class: both canonical systems use r_p=2. One passing predicate alone suffices. |
| Interpret two-root existence as both single-root systems complete | Disjoint theorem uses OR. Separator theorem lets the covering side vary with the common exponent coordinate, not with reselected states. |
| General trace iff is advertised as a complete order-only C9 | Explicitly not claimed; a lower state vector remains existential. |
| Last q2 dynamic component fills a missing log | Its own prime coordinate is free below q2 and has a safe center; larger regular rows cannot remove that escape. |
| Choose a new lower state for each q2 log | D1 fixes ONE tau before forming the entire trace. |
| Two root logs need only mod-q compatibility at arbitrary accepted h | D1 checks modulo q^h, h the smallest accepted rigid valuation. |
| Pairwise congruence tests ignore next-digit obstructions | Center roles are nested residue equalities. Arbitrary exact-valuation demands retain the source C3 forbidden-digit criterion. |
| All supply sets are exact masks, suppressing extra fatal events | Not assumed. Frozen-state iff uses FULL ACTUAL rigid sets. Free-state sufficiency can use supplied subsets because extra coverage is harmless. |
| Ignore a nonregular root reached through the other closure | Separator/disjoint results exclude this case. D1 handles it; D2 gives a serial subclass iff. |
| s_q1 not accepted, yet q2 constrains q1-center choices | All q1 FIRST digits have safe lifts; more than two q2 log projections survive. |
| Merely disjoint regular row labels imply CRT independence | False if free coordinates or root coordinates are shared. D4 requires disjoint odd DEPENDENCY COORDINATES. |
| One shared relay p can be detached from its lower order factors | Only p=3 can be a singleton complete hereditary regular intersection. A p>3 relay carries its common lower closure. |
| Common-factor width test applied to comparable roots | D5 is restricted to incomparable roots with regular relevant closures. |
| Local collision on two fibers proves arbitrary global no-go | It proves no frozen-state completion when those exact demands are mandatory. Known roots 20771/40487 already have source P4 exclusion. |
| Different anchor roles necessarily demand different r | Actual p7,r2 gives compatible anchor0 and anchor1 centers at d0 and d21. |
| Resultant divisibility at two fibers implies state reuse | The literal large gcd contains 20771, but u=155 does not divide the lower difference1. |
| The gcd computation is a bounded prime scan | It is exact arithmetic on two specified integers, with a complete small-prime factorization of their gcd. |
| All-prime simultaneous-use exclusion assumes a full factorization of Phi67(5) | It only uses exact gcd(Phi67(5),large_resultant_gcd)=269*1609 and both factors' nonadmissibility. |
| OR alternatives are all simultaneously mandatory | Each collision/gcd cut is conditional on a selected conjunction or a genuinely unavoidable allocation. |
| A current macro rank is put into an original resultant | All numerical h=67 calculations use the original q20771 order decomposition. |
| A two-root whole-cover no-go eliminates two seven-basin leaves | No: those two basins need supply assigned leaves, not cover the whole domain themselves. |
| Exactly-seven still has a shared admitted relay above3 | Source normal form already forbids it; remaining separator is3/parity. |
| The conditional odd-s relay example is actual | No such prime family has been instantiated. |
| The exactly-two LP gap has interacting nonregular rows | The added rows are explicitly inert; this is only a relaxation regression. |
| Two exact resultant organizations are independent software stacks | They run in one SymPy/Python environment and are not claimed independent. |
| Repository status or scope was changed | Only read actions were invoked. No GitHub write, repository-native run, or route promotion. |

Highest-value independent proof reviews: D1's complete terminal trace,
D2's upward-safe extension after a nonregular row, D3's hereditary separation
and full-actual-vs-supply distinction, and D4's fixed-parity C7 argument.
