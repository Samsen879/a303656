# Exact P5 sparse cross-class panel feasibility audit

## Scope and authority

The run began at exact commit
`c710dcc9b472a8500b038a33db1315043cffca6e`, after verifying the required
three-commit chain and direct-parent relation. Source commit Q is
`4605e09047bc8ca934392c3e26a3ec8f7e8c7370`.

The frozen fifth-prime shortlist, its global summaries and compressed
landscapes, the corrected 13-mask holdout manifest, fixed-P4 tuple authorities,
and all earlier certified finite registries were hash-checked before and after
the computation.

No `T(n)`, winner mask, factorization, two-square representability, Backend A,
Backend B, or direct oracle was evaluated. The integers below are only exact
members of modular classes in the activation cell.

## Complete prior-evaluation exclusion

The builder reads the final formal-results audit, both contiguous T pilots, the
selected dossier, corrected P2 controls, the complete K4 progression panel, the
fixed-P4 maximizer census, and all direct-oracle representative manifests. It
also verifies that the K4/P4 direct subsets are contained in their complete
panels and records the later no-new-integer audits.

After merging inclusive overlap/adjacency and deduplicating sparse points:

| Registry quantity | Exact value |
|---|---:|
| interval-union components | 28 |
| distinct sparse points | 6,129 |
| normalized digest | `0fad117f1fa414e438d86bbd2f9ef76a91649bd1f88cd9f8cfcefb78fdd29cd7` |

## Families and anchors

Family R uses the single frozen representative `t`. Family U uses every
residue with the same resulting union mask. Family Q uses every same-q
marginal-optimal residue and retains its different union masks separately.
Family Q is feasibility comparison only.

The six authenticated global-optimum anchors are:

| Anchor | Entry | q | t | R/U/Q residue counts |
|---|---|---:|---:|---:|
| G231 common | ENTRY-001 | 19 | 12 | 1 / 11 / 19 |
| pooled/global weighted | ENTRY-005 | 19 | 35 | 1 / 9 / 9 |
| t3=2 constrained weighted | ENTRY-006 | 19 | 38 | 1 / 8 / 8 |
| CAL-216 | ENTRY-002 | 19 | 12 | 1 / 12 / 12 |
| CAL-227 | ENTRY-003 | 19 | 12 | 1 / 11 / 20 |
| CAL-230 | ENTRY-004 | 19 | 12 | 1 / 11 / 30 |

These values were read from the authenticated global results; equality was not
assumed.

## Exact multiplicity and freshness

| Family | Raw/distinct classes | Occupied | Previously evaluated | Fresh occupied | s_all | s_anchor |
|---|---:|---:|---:|---:|---:|---:|
| R | 822 | 63 | 26 | 37 | 0 | 0 |
| U | 24,694 | 1,029 | 572 | 457 | 0 | 0 |
| Q | 35,324 | 1,523 | 751 | 772 | 0 | 0 |

All 60,840 raw class descriptions are distinct CRT classes. The 2,615 occupied
descriptions reduce to 1,475 distinct integers after cross-entry/family integer
deduplication; 1,140 are duplicate integer descriptions. There are 33 distinct
resulting union masks and all 20 shortlist provenance identities remain.

Threshold counts for every entry and the six-anchor subset are recorded in
`feasibility_report.json`; all per-entry counts reconstruct from the class and
integer manifests.

## Provisional designs and geometry

Because `s_all=0` and `s_anchor=0` for all three families, the minimum-eight
preconditions for Designs A and B fail. Design C therefore contains every fresh
Family-U occupied class, with exact per-entry imbalance reported.

The verifier independently uses a direct CRT sum, checks every congruence, and
confirms that the cell width is smaller than each `L5(q)`, so no class has more
than one member. Exact class-residue alignment and member-position digests are
committed. Only `R/ENTRY-008` has more than half of its occupied members within
the first or last five percent of the cell under the predeclared diagnostic.
No random-sampling or independence claim is made.

## Verification

The unit suite passed. All 15 required corruption tests returned code 2,
including missing registry components, shortlist/base/union mutation,
unauthorized residue, CRT and boundary corruption, missed class/integer
deduplication, incorrect freshness, multi-member claims, adaptive deletion,
non-deterministic selection, and a forbidden-evaluator invocation flag.

Validation conclusion:

**VALIDATED EXACT P5 SPARSE CROSS-CLASS PANEL FEASIBILITY AUDIT**

Interpretation label:

**ONLY AN UNBALANCED FULL SPARSE CENSUS IS FEASIBLE**

The global mathematical problem remains **UNRESOLVED**.
